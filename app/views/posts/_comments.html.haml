- comments.each do |comment|
  %li.list-group-item.p-2
    = comment.content
    = link_to '編集', edit_post_comment_path(post, comment)
    = button_to '削除', post_comment_path(post, comment), method: :delete, class: 'btn btn-outline-danger'
